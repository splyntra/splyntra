apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "splyntra.fullname" . }}-security
  labels:
    {{- include "splyntra.labels" . | nindent 4 }}
    app.kubernetes.io/component: security
spec:
  replicas: {{ .Values.security.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: security
  template:
    metadata:
      labels:
        {{- include "splyntra.labels" . | nindent 8 }}
        app.kubernetes.io/component: security
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets: {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: security
          image: {{ include "splyntra.securityImage" . }}
          imagePullPolicy: {{ .Values.images.security.pullPolicy }}
          ports:
            - containerPort: {{ .Values.security.service.port }}
          envFrom:
            - secretRef:
                name: {{ include "splyntra.secretName" . }}
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.security.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 15
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.security.service.port }}
            initialDelaySeconds: 20
            periodSeconds: 20
          resources: {{- toYaml .Values.security.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "splyntra.fullname" . }}-security
  labels:
    {{- include "splyntra.labels" . | nindent 4 }}
    app.kubernetes.io/component: security
spec:
  type: {{ .Values.security.service.type }}
  selector:
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: security
  ports:
    - port: {{ .Values.security.service.port }}
      targetPort: {{ .Values.security.service.port }}
      name: http
