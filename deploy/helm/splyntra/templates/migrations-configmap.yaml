apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "splyntra.fullname" . }}-pg-init
  labels:
    {{- include "splyntra.labels" . | nindent 4 }}
data:
{{ (.Files.Glob "files/postgres/*.sql").AsConfig | indent 2 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "splyntra.fullname" . }}-ch-init
  labels:
    {{- include "splyntra.labels" . | nindent 4 }}
data:
{{ (.Files.Glob "files/clickhouse/*.sql").AsConfig | indent 2 }}
