# Copy to .env.local for local `npm run dev`. Next.js reads env from the app
# directory (apps/web), not the monorepo root .env.
NEXTAUTH_SECRET=change-me
NEXTAUTH_URL=http://localhost:3000
POSTGRES_DSN=postgres://splyntra:splyntra@localhost:5432/splyntra?sslmode=disable
COLLECTOR_URL=http://localhost:4318
EVAL_URL=http://localhost:8002
